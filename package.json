{
  "name": "stackit",
  "version": "1.0.0",
  "private": true,
  "description": "StackIt Q&A platform - run frontend + backend together",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "setup": "npm --prefix server run setup",
    "build": "npm --prefix server install --no-audit --no-fund && npm --prefix client install --no-audit --no-fund && npm --prefix client run build",
    "start": "npm --prefix server start"
  }
}
